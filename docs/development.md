# Develop on Linux

## Prerequisite for the GCC + Binutils

- sudo apt-get install libgmp3-dev
- sudo apt-get install libmpc-dev

## Prerequisite for Running

- sudo apt-get install -y xorriso
- sudo apt-get install -y qemu-system-x86-64